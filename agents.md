# 行为规范

- 以瞎猜接口为耻，以认真查询为荣。
- 以模糊执行为耻，以寻求确认为荣。
- 以臆想业务为耻，以人类确认为荣。
- 以创造接口为耻，以复用现有为荣。
- 以跳过验证为耻，以主动测试为荣。
- 以破坏架构为耻，以遵循规范为荣。
- 以假装理解为耻，以诚实无知为荣。
- 以盲目修改为耻，以谨慎重构为荣。

## STermux 项目约束

- 开始任何任务前，先阅读 `PROJECT_PLAN.md`。
- 严格按照当前 Phase 工作，不提前实现后续阶段。
- 修改前先检查现有代码和目录结构，避免重复实现。
- 涉及 SillyTavern 路径、数据目录、启动方式和更新方式时，必须先确认当前实际结构，不凭记忆假设。
- 涉及用户数据的复制、覆盖、删除、恢复操作时，优先保证数据安全。
- 任何删除操作必须验证目标路径，禁止对未验证变量直接执行递归删除。
- 手动备份不得被自动清理机制删除。
- 批量更新第三方扩展时，单个扩展失败不得中断其他扩展。
- 标记为“仅手动更新”的扩展不得参与一键批量更新。
- 网络功能失败不得影响 SillyTavern 本地启动、备份查看和恢复功能。
- 每个阶段完成后必须执行 Bash 语法检查，并提供 Termux 实机测试步骤。
- 未经明确要求，不进行大规模架构重构。
- 优先实现简单、可靠、可维护的方案，避免无实际需求的复杂抽象。

## 测试强制约束

- 所有修改完成后，先执行当前环境能够执行的最高级别验证。
- 能自动测试的功能不得只依赖人工检查。
- Termux 专属功能未经 Android Termux 实机验证，不得标记为“已完成”。
- 无法执行实机验证时，必须标记为“待 Termux 实机验证”，并提供明确测试步骤。
- 自动测试必须使用隔离测试目录，禁止操作用户真实 SillyTavern 数据和真实备份。
- 自动测试不得复制、读取、source 或使用项目真实 `config/user.conf`；测试配置必须由测试自身在隔离目录创建。
- 自动测试不得启动真实 SillyTavern；所有启动测试必须使用带有违规启动哨兵的 Fixture 验证隔离边界。
- SillyTavern 安装自动测试不得访问真实网络、调用真实 `pkg` 或写入测试临时目录之外；必须使用 Mock 命令和本地临时 Git 仓库。
- STermux 自更新测试不得对真实 STermux 仓库执行 fetch、pull、checkout 或重启；必须使用临时本地 Git 仓库和可替换的重启函数。
- 每个 Phase 的阶段成果总结必须附带测试矩阵。
- 用户实机测试失败后，必须记录问题、修复并重新验证，不得直接跳过。
